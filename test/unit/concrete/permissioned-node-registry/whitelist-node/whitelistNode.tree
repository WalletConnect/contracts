whitelistNode.t.sol
├── when the caller is not the owner
│   └── it should revert
└── when the caller is the owner
    ├── given the node is already whitelisted
    │   └── it should revert
    └── given the node is not whitelisted
        ├── given the node is the zero address
        │   └── it should revert
        └── given the node is not the zero address
            ├── it should whitelist the node
            ├── it should emit a NodeWhitelisted event
            └── it should increment the whitelist nodes count
