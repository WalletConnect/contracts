forceWithdrawAll.t.sol
├── when caller is not admin
│   └── it should revert with access control error
└── when caller is admin
    ├── when to address is zero
    │   └── it should revert with invalid address error
    └── when to address is valid
        ├── when stake weight is paused
        │   └── it should revert with paused error
        └── when stake weight is not paused
            ├── when user has no lock
            │   └── it should revert with non existent lock error
            └── when user has lock
                ├── when lock has no transferred amount
                │   └── it should not transfer any tokens
                └── when lock has transferred amount
                    ├── it should transfer tokens to recipient
                    ├── it should clear the lock
                    ├── it should update supply
                    └── it should emit correct events
