unstake.t.sol
├── given the pauser has staking paused
│   └── it should revert
└── given the pauser has staking unpaused
    ├── when the caller unstake amount is zero
    │   └── it should revert
    ├── when the caller unstake amount is gt their stake
    │   └── it should revert
    ├── when the caller unstake amount is lt their stake
    │   ├── when the unstake amount will put the stake below the minimum
    │   │   └── it should revert
    │   └── when the unstake amount will not put the stake below the minimum
    │       ├── it should decrease the caller's stake by the unstake amount
    │       ├── it should emit an {Unstaked} event
    │       └── it should transfer the unstake amount of tokens from the contract to the caller
    └── when the caller unstake amount is eq their stake
        ├── it should decrease the caller's stake to zero
        ├── it should emit an {Unstaked} event
        └── it should transfer the unstake amount of tokens from the contract to the caller
