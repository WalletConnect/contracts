withdrawAll.t.sol
├── given contract is paused
│   └── it should revert with Paused error
└── given contract is not paused
    ├── when user has no lock
    │   └── it should revert with NonExistentLock error
    ├── when user has a lock but it hasn't expired
    │   └── it should revert with LockStillActive error
    └── when user has an expired lock
        ├── it should transfer the full amount to the user
        ├── it should set the lock amount to zero
        ├── it should set the lock end to zero
        ├── it should update the supply
        ├── it should emit a Withdraw event
        └── it should emit a Supply event
